var Commentaire = require('../models/comments.js');

/*
exports.index = function(req, res){
	Commentaire
		.find()
		.exec(function(err, docs) {
		if(err) {
			throw err;
		} else if( (docs) && (docs.length) ) {
			res.render('inc/aside', { title: 'Commentaires', comments: docs});
		} else {
			res.render('inc/aside', { title: 'Commentaires', comments: null});
		}
	});
}
*/
